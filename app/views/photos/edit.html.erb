<h1>Editing photo</h1>

<div class="row">
  <div class="col-md-6">
    <%= render 'form' %>

    <%= link_to 'Show', user_photo_path(@user, @photo) %> |
    <%= link_to 'Back', user_photos_path(@user) %>
  </div>
</div>
